# Deployment Guide

## Community Cloud
Push to GitHub, create a Streamlit Community Cloud app, select `app.py`, and deploy. No secrets are needed for the default mode.

## Private deployment
Use Docker behind an organisation-approved identity, logging and network layer. Conduct privacy, security, legal and clinical-safety review before allowing real data.
